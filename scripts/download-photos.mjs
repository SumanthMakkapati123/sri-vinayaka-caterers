import {readFile,writeFile,mkdir} from 'node:fs/promises';
const research=JSON.parse(await readFile('research/photo-candidates.json','utf8'));
const picks=[['gulab-jamun','File:Two Gulab Jamun in a plate 01.jpg'],['rava-kesari','File:Kesari Bath (5832083727).jpg'],['pala-payasam','File:Rice pudding also called payasam or kheer.jpg'],['gobi-65','File:Gobi 65 (cauliflower fry).jpg'],['palak-pakoda','File:Palak Pakoda (Spinach Pakoda).JPG'],['veg-biryani','File:Vegetable Biryani IMG 001.jpg'],['lemon-rice','File:Picture of Lemon rice dish.JPG']];
await mkdir('research/photo-review',{recursive:true});const records=[];
for(const [id,title] of picks){const p=research.flatMap(r=>r.candidates).find(p=>p.title===title);if(!p)continue;const meta=p.extmetadata;const license=meta.LicenseShortName?.value;if(!/^(CC BY-SA|CC BY|CC0|Public domain)/.test(license)){console.log('Skipped license '+title);continue;}const res=await fetch(p.thumburl,{headers:{'User-Agent':'SriVinayakaCatalog/1.0'}});if(!res.ok){console.log('Download unavailable '+res.status+' '+title);continue;}await writeFile(`research/photo-review/${id}.jpg`,Buffer.from(await res.arrayBuffer()));records.push({id,file:`dish-${id}.jpg`,creator:meta.Artist.value.replace(/<[^>]*>/g,''),source:p.descriptionurl,license,licenseURL:meta.LicenseUrl?.value,changes:'Resized by Wikimedia; displayed cropped to fit dish cards.',original:p.url});console.log('Downloaded '+id);await new Promise(resolve=>setTimeout(resolve,1500));}
await writeFile('research/selected-photos.json',JSON.stringify(records,null,2));
